# agent.md

本文件记录 agent 在 `D:\code\reconstructOneFrame` 项目中的工作原则、设计约束、验证口径和提交要求。

## 项目定位

`reconstructOneFrame` 是对 `D:\code\teethscanalgorithm3x4` 的重构项目。

`D:\code\teethscanalgorithm3x4` 仅作为 Legacy 对比基线使用，不是必须沿用的实现模板。重构目标不是等价迁移，而是在精度、速度、功能完整性、可维护性和可验证性上全面超越 Legacy。

Legacy 可以作为以下用途：

- 对比输入/输出契约、配置含义、运行入口、数据集组织方式和历史功能覆盖面。
- 提供精度、速度、点云质量、阶段输出和功能完整性的基准。
- 帮助识别不可丢失的业务能力、硬件约束和下游依赖。
- 作为回归对照，而不是架构约束。

## 第一原则：大胆掀桌子

本项目不遵循“最小改动原则”。

当 Legacy 的结构、命名、数据流、状态管理或模块边界阻碍重构目标时，应优先重建清晰架构，而不是围绕旧代码做局部补丁。

允许并鼓励：

- 重划模块边界。
- 重定义内部数据结构。
- 重写核心流程。
- 删除历史包袱、隐式状态和难以验证的旁路逻辑。
- 用清晰、可测、可扩展的新实现替代旧实现。

但“掀桌子”不等于无证据重写。任何大范围重构都必须先说明：

- 要替代的 Legacy 行为是什么。
- 新设计的数据输入、数据输出和状态码契约是什么。
- 新设计如何证明功能不丢失。
- 新设计如何在精度、速度或可维护性上超越 Legacy。

## 第二原则：模块化设计，概要设计先行

任何庞大功能或跨模块改动，必须先形成概要设计，再进入实现。

概要设计至少包含：

- 模块职责边界。
- 模块输入数据结构。
- 模块输出数据结构。
- 状态码、错误码和诊断信息。
- CUDA/CPU 分工。
- 数据所有权和生命周期。
- 关键性能路径。
- 与 Legacy 的对比点。
- 验证方法和验收标准。

模块之间只通过明确的数据输入、数据输出、状态码和配置参数交互。禁止依赖隐式全局状态、跨模块直接改写内部对象、隐藏副作用或不可追踪的共享缓存。

推荐模块形态：

- `config`：配置加载、默认值、合法性检查、版本兼容。
- `io`：图像、标定、PLY、调试图、阶段产物读写。
- `calibration`：标定参数解析、几何契约、坐标系说明。
- `phase`：相位求解、质量评估、unwrap、可靠性输出。
- `matching`：立体匹配、视差、唯一性、左右一致性、连续性约束。
- `reconstruction`：三维重建、坐标转换、法向、颜色绑定。
- `filtering`：点云过滤、连通域、离群点、边界处理。
- `metrics`：精度评估、速度评估、Legacy 对比。
- `diagnostics`：调试图、阶段统计、失败原因定位。
- `app`：命令行入口、运行编排、退出码。

模块接口应优先稳定，模块内部可以大胆演进。

## 第三原则：优先 CUDA 编码测试

本项目优先实现和验证 CUDA 路径。

对于相位计算、unwrap、匹配、重建、过滤、质量图和批量像素操作等高吞吐模块，应优先考虑 GPU 数据流和 CUDA kernel 设计。

默认要求：

- 新增核心计算能力时，优先设计 CUDA 版本。
- CPU 版本主要用于参考实现、调试、单步验证或无 GPU 环境 fallback。
- CUDA 输出必须可与 CPU 参考实现或可解释的数学基准对比。
- CUDA kernel 要关注内存访问模式、分支发散、重复分配、host-device 拷贝和同步点。
- 不允许为了方便调试把生产热路径长期退回 CPU。

CUDA 验证至少覆盖：

- 输出数值范围。
- NaN/Inf/无效值处理。
- mask 和状态码一致性。
- 与 CPU 参考结果的误差统计。
- 典型数据集运行时间。
- 边界输入、空输入、低纹理、饱和、高噪声等异常情况。

## 第四原则：全面超越 Legacy

`D:\code\teethscanalgorithm3x4` 是 Legacy 对比基线。本项目的验收不能只停留在“能跑通”。

每个重要模块应至少从以下维度对比 Legacy：

- 精度：点云误差、平面拟合、台阶高度、局部连续性、离群点比例。
- 速度：单帧耗时、核心阶段耗时、GPU/CPU 同步成本、内存分配成本。
- 功能完整性：Legacy 已有能力是否覆盖，新能力是否补齐历史短板。
- 鲁棒性：低质量帧、局部饱和、遮挡、相位不稳定、匹配歧义等场景表现。
- 可诊断性：失败时能否输出明确状态码、阶段统计和必要调试产物。
- 可维护性：模块是否独立，接口是否清晰，测试和运行入口是否稳定。

禁止只用单张肉眼图、单次成功运行或单个平均值证明超越。需要尽量提供脚本化统计、路径明确的输出产物和可复现命令。

## 第五原则：TDD 不优先

本项目不把 TDD 作为最高优先级。

当功能庞大、算法链路尚未稳定、CUDA kernel 仍在探索或模块边界正在重划时，可以先完成概要设计、核心实现和实验验证，再补充测试。

但这不代表不要测试。最低要求是：

- 核心模块必须有可重复运行的验证命令。
- 关键数值结果必须有误差统计或阈值判断。
- CUDA 与 CPU 或 Legacy 对比必须能复现。
- 对外接口、配置解析、状态码和错误路径应补充轻量测试。
- 修复明确 bug 时，应优先增加能覆盖该 bug 的回归验证。

TDD 可以让位于重构速度，但验证证据不能缺席。

## Legacy 约束吸收

Legacy 项目中已记录的硬件、算法和验证约束应作为背景知识吸收，但不得机械复制旧实现。

需要保留的认知包括：

- 这是结构光双目重建链路，必须关注实时重建热路径。
- 标定加载、极线校正画布搜索、map 生成和重型诊断应放在初始化或预处理阶段，不应进入逐帧热路径。
- 逐帧流程应复用预计算 map、mask、buffer、投影矩阵和 GPU 内存。
- 二值条纹通过光学离焦近似正弦强度，不能假设理想正弦或理想 gamma 线性相移样本。
- 相位质量、unwrap 可靠性、匹配唯一性、左右一致性和空间连续性都应作为可诊断信号显式输出。
- 稀疏但可靠的点优先于密集但分层或歧义的点。
- 生产路径应优先优化快速 disparity 路径；诊断性或兜底路径不能轻易升级为默认生产路径。

本项目可以重新设计上述能力，但必须覆盖对应问题。

## 数据契约与状态码

所有模块应通过显式契约交互。

输入数据应明确：

- 尺寸。
- 类型。
- 通道数。
- 坐标系。
- 单位。
- 有效 mask。
- 所属相机或阶段。
- 是否已校正、归一化、滤波或转换。

输出数据应明确：

- 数据语义。
- 有效范围。
- 无效值表达方式。
- 状态码。
- 统计摘要。
- 可选诊断产物路径。

状态码应区分：

- 成功。
- 输入缺失。
- 配置非法。
- 标定非法。
- 尺寸不匹配。
- CUDA 初始化失败。
- CUDA kernel 失败。
- 数据质量不足。
- 相位失败。
- unwrap 失败。
- 匹配失败。
- 重建点数不足。
- 输出写入失败。

禁止只返回 `bool` 或只打印日志来表达复杂失败原因。

## 性能原则

实时路径应优先满足：

- 少分配。
- 少拷贝。
- 少同步。
- 少分支。
- 少日志。
- 多复用。
- 明确缓存生命周期。

任何新增热路径日志、阶段输出、debug map、CPU fallback 或额外统计，都应默认可关闭，并说明运行成本。

性能验证应至少记录：

- 总耗时。
- 各主要阶段耗时。
- GPU kernel 耗时。
- host-device 拷贝耗时。
- 内存分配次数或可疑分配点。
- 与 Legacy 的同数据对比。

## 精度与质量验证

精度验证应优先使用可复现脚本，而不是人工观察。

常用验证方向：

- 标定参数加载一致性。
- 相位图连续性和残差统计。
- unwrap integer 连续性。
- 视差连续性。
- 左右一致性。
- 点云有效点数。
- 平面拟合残差。
- 台阶高度误差。
- 离群点比例。
- 法向方向和非零比例。
- PLY/component-wise 数值比较。

当 Legacy 输出存在已知缺陷时，不应为了贴近 Legacy 而复制缺陷。应记录差异原因，并用更强的几何、统计或下游验证证明新结果更合理。

## 文档要求

庞大功能、跨模块重构、算法路线变化和影响 Legacy 对比口径的修改，都应先写设计文档或计划文档。

文档应包含：

- 背景问题。
- Legacy 当前行为。
- 新方案目标。
- 模块划分。
- 数据契约。
- CUDA/CPU 设计。
- 风险点。
- 验证命令。
- 验收标准。
- 未决问题。

文档应放在项目内稳定路径，优先使用 `docs/` 或 `docs/planning/`。

## 提交准备要求

准备提交信息时，不要自动提交，除非用户明确要求提交。

默认偏好是单个合并提交。应先给出一个完整的单提交信息，再简要分析是否有必要拆分。只有当变更明显无关、审查风险较高或用户要求拆分时，才建议拆成多个提交。

提交总结必须基于明确的 Git 检查结果，不能只依赖对话记忆。准备提交信息前，应运行并参考：

```powershell
git status --short
git diff --stat
git diff
git diff --cached
```

如适用，还应检查：

```powershell
git branch --show-current
git status --branch --short
```

提交前说明必须列出：

- 准备纳入提交的文件，按新增、修改、删除分组。
- 明确排除的文件及原因。
- 不应提交的未跟踪文件或生成产物。
- 已运行的验证命令和结果。
- 未运行验证的原因。

提交信息使用中文，除非用户另有要求。

提交信息使用 Conventional Commits 格式：

```text
type(scope): 简短摘要

正文说明改了什么、为什么改、如何验证。
```

推荐类型：

- `feat`：新增能力。
- `fix`：修复缺陷。
- `refactor`：重构，不改变外部功能。
- `perf`：性能优化。
- `test`：测试或验证脚本。
- `docs`：文档。
- `build`：构建系统。
- `chore`：工程维护。

除非用户明确要求，不要执行 `git commit`、`git push`、大范围 reset、清理未跟踪文件或覆盖外部运行目录。

## 工作方式

默认工作顺序：

1. 先确认问题目标和 Legacy 对比对象。
2. 快速检查当前目录、分支、已有文档和相关代码。
3. 对庞大功能先写概要设计。
4. 明确模块契约和状态码。
5. 优先实现 CUDA 路径或 CUDA 可验证骨架。
6. 用脚本化方式对比 Legacy。
7. 输出精度、速度、功能完整性和风险结论。
8. 最后再整理提交信息或执行用户明确授权的提交。

当用户要求“先分析”“先设计”“暂不修改代码”时，只做分析或设计，不擅自实现。

当用户要求实现时，应优先形成可运行、可验证、可对比的工程结果，而不是只给建议。
